# 项目架构与 AI 行为准则

本项目采用 **Next.js (React) + Node.js + Supabase** 架构。以下规则为本项目中 AI 助手所有行为的最高准则，须完整遵守。

---

# 核心执行准则

1. **文档强制读取**：每次新建对话或开发新功能前，必须优先读取 `/.cursor/docs/` 目录下的 4 个核心架构文档，确保上下文同步。

2. **强制反问机制**：在进行功能设计或代码编写前，若发现需求模糊、缺少边界条件或架构存在技术冲突，必须停止生成代码，并以列表形式向用户提问，待确认后方可继续。

3. **架构变更熔断与级联风险评估**：当需要修改基础架构或数据库时，严禁直接修改代码或文档。必须先输出结构化的《影响评估报告》，在获得明确同意后方可执行。

4. **数据库安全与权限隔离**：涉及 Supabase 数据库表结构的修改必须生成标准的 SQL 迁移脚本（Migration）。在业务代码查询时，严禁使用 `Service Role Key` 绕过权限，必须强制依赖 RLS (Row Level Security) 确保多租户数据隔离（除特殊后台 Job 外）。

5. **死锁解决与防呆机制**：当同一模块的调试报错达到 3 次时，立即触发死锁解决机制。严禁继续盲目生成修补代码。必须优先输出当前理解的系统调用栈和数据流向图（纯文本形式），待用户确认逻辑理解无误后，再探讨修复方案。

6. **环境驱动与无硬编码**：绝对禁止在代码中硬编码任何凭证、API 密钥、数据库连接串。大语言模型 (LLM) 和向量模型 (Embedding) 的配置（包括模型名称、本地/云端 API Endpoint）必须从 `.env` 动态读取，严禁在业务逻辑中写死。

7. **强制类型安全与测试防御**：所有前端和后端代码必须使用严谨的 TypeScript 编写，杜绝滥用 `any`。每次开发核心业务模块时，必须同步生成对应的自动化测试用例（如 Jest/Vitest），确保核心逻辑的分支覆盖率。

8. **架构可视化呈现规范**：如需在文档中展示系统架构、时间轴或矛盾矩阵等图表，严禁使用 Mermaid 语法。必须强制输出高质量的 HTML 和 CSS（如 CSS Grid/Flexbox）代码来实现可视化，确保文档打印与渲染的绝对稳定。注意：内部提示词或代码注释中严禁出现特定品牌标识，品牌标识仅限于最终交付给客户的可视化文档中出现。

9. **精准上下文锚定**：在响应需求时，必须主动要求用户通过 @ 指令提供必要的文件上下文（如 @01_PRD_AND_UI.md 或相关的具体代码文件）。严禁在没有明确文件引用的情况下盲目推测现有代码结构。

10. **本地容器化优先原则**：本项目最终目标为离线本地部署。所有外部依赖（包含 Supabase 实例、PostgreSQL pgvector 扩展、本地 AI 模型推理服务等）必须具备 Docker 化部署能力。严禁引入仅依赖公有云且无法本地替代的第三方闭源强依赖。

11. **RAG 与 AI 调用规范**：涉及公有 API（如 Gemini API）或本地模型调用时，必须支持流式输出 (Server-Sent Events/Streaming)。RAG 向量存储必须严格使用 Supabase 的 pgvector 扩展，严禁引入额外的向量数据库组件增加系统复杂度。

12. **强制使用 Supabase CLI**：所有数据库表结构变动，严禁手动创建 SQL 文件。必须强制输出 `supabase db push的执行命令，并将生成的 SQL 代码写入对应的时间戳文件中。

13. **本地 AI 接口标准化**：调用本地大语言模型时，默认所有本地服务（如 Ollama）均对外暴露完全兼容 OpenAI API 规范的 Endpoint，严禁使用非标准 SDK 构建调用逻辑。
14. **UI开发**：ui使用ShadcnUI组件库开发。对应行业审美的要求，保持简洁的企业级应用。同时满足浏览器和手机，pad等移动端的显示优化。
