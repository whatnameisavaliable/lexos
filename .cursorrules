# Cursor System-Level Behavioral Rules & Constraints

## 1. 核心开发前置流程 (Mandatory Execution Flow)
* **强制前置阅读**：在执行任何编码、重构或修改逻辑的任务前，你必须首先且完整地阅读项目根目录下的 `Architecture.md`。你必须严格对齐白皮书中的目录结构、通信契约与技术规范。
* **变更审查**：每次接受新需求，必须先检查代码库中现有的依赖及表结构。如果发现逻辑漏洞或不符合工程常理的设计，必须立即向用户发出警告并拒绝执行，严禁强行编码。

## 2. 数据库变更红线 (Database Schema Modification Line)
* **禁止直接修改**：严禁通过任何方式直接绕过迁移脚本去修改线上或本地 Supabase 数据库的 Schema。
* **迁移文件唯一性**：所有的数据库表结构变更（包括新增字段、修改索引、调整 RLS 策略等），必须通过编写 Supabase SQL Migration 脚本来实现。修改完成后必须提示用户运行 `supabase db push`。
* **数据安全隔离**：任何新创的数据表，必须默认开启 RLS (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY;`)。必须显式定义基于 `auth.uid()` 的安全策略。

## 3. 前端排版与渲染规范 (Layout & Visual Controls)
* **复杂布局约束**：对于系统内的案件时间轴、文书生成对比、矛盾矩阵等法律业务核心可视化输出，必须且只能使用 HTML 和 CSS (Grid 或 Flexbox) 进行实现。
* **打印稳定性要求**：禁止引入任何不稳定的第三方图表库（例如 Mermaid 语法或动态 DOM 节点计算库）。所有布局设计必须保证在 A4 纸张打印及 PDF 导出时的绝对像素级对齐，防止出现任何跨页截断或溢出错误。
* **实体名称脱敏隔离**：在编写内部工具提示词、系统核心状态管理、后端通用逻辑流转的代码或注释时，严禁硬编码特定的律师事务所实体名称（如“上海申浩（天津）律师事务所”）。此类实体标识必须作为系统配置项、前端独立视图层组件或动态变量进行管理。但须确保这些标识在面向最终客户的 PDF 导出组件和对外可视化报告中呈现。

## 4. 后端编码与异常分支规范 (Backend Risk & Error Controls)
* **异常分支兜底**：编写任何网络请求、数据库读写、AI 接口调用的 Server Actions 或 API Routes 时，必须包含完整的 `try-catch` 块。
* **网络超时与空数据**：必须显式处理网络超时（默认 30s 熔断）与空数据状态（Empty State）。在前端对应的 UI 组件中，必须显式编写兜底图表或加载状态（Skeleton 骨架屏）。
* **风险控制点 (Risk Control)**：在注释及代码结构中，处理法律逻辑或业务风控的关键节点处，必须统一使用 `// RISK CONTROL: [风控描述]` 或 `/** 风险控制点: ... */` 作为标准注释前缀。严禁使用“质量控制（Quality Control）”等非专业工程术语。
* **审计级日志**：针对所有数据变更（CUD 操作）、涉及香港客户的跨境数据流转及敏感 API 调用，必须在捕获异常或成功执行时，使用系统标准的审计日志结构进行记录。结构至少包含：`timestamp`, `operator_id`, `action_type`, `resource_id`, `context`。若字段不可获取，必须填充 `null`。