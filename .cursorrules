# 项目规则与开发标准

## 核心技术栈
- Next.js v14 (App Router)
- Node.js v20
- Supabase JS Client v2

## 严格技术约束

### 1. 前端排版与 UI
- **强制要求**：所有布局和对齐必须排他性地使用 **CSS Grid** 或 **Flexbox**。
- **严厉禁止**：**严禁使用绝对定位（`position: absolute`）**，以确保最大程度的布局稳定性与可靠性。

### 2. 组件架构
- **强制要求**：所有新页面和组件默认优先使用 **React Server Components (RSC)**。
- **限制条件**：仅在严格需要用户交互（如 useState, useEffect）或仅限客户端的 Web API 时，方可使用 `'use client'`。

### 3. 代码可维护性
- **硬性上限**：**单个文件的代码量绝对不得超过 300 行**。
- **执行动作**：接近或超过 300 行的文件必须立即拆分为模块化的子组件或独立的辅助函数。

### 4. TypeScript 标准
- **强制要求**：必须严格遵守 TypeScript 严格模式（`tsconfig.json` 中必须强制启用 `"strict": true`）。
- **严厉禁止**：严禁使用 `any` 类型。所有变量、函数参数和组件 Prop 必须进行明确且严格的类型定义。
