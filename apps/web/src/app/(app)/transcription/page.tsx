import { TranscriptionTasksPanel } from "@/components/transcription/transcription-tasks-panel";

/** 律师 — 语音转写任务列表（`ui_design.md` §6.3）。 */
export default function TranscriptionPage() {
  return <TranscriptionTasksPanel />;
}
