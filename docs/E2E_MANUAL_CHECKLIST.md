# LexOS E2E 人工验收清单

| 字段 | 内容 |
|------|------|
| 版本 | 1.0 |
| 用途 | 无法完全自动化的端到端验收步骤 |
| 前置 | U1/U2/U3 进程已按 `DEPLOYMENT.md` 启动；测试账号已种子 |

**记录方式**：每项填写 `通过` / `失败` / `跳过` + 备注 + 验收人 + 日期。

---

## 1. 认证与 MFA

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 1.1 | 使用错误密码连续登录 5 次 | 账户锁定或 Auth 限流生效 | | |
| 1.2 | admin 账号绑定 TOTP MFA（真机扫码） | 绑定成功；后续登录需 TOTP | | |
| 1.3 | admin MFA 登录后访问 `/admin/users` | 正常进入用户管理 | | |
| 1.4 | 律师账号登录（无 MFA） | 正常进入业务区或强制改密页 | | |
| 1.5 | 禁用正在会话中的律师；律师下一请求 | 会话失效，跳转登录 | | |
| 1.6 | `CAPTCHA_PROVIDER=none` 环境：外网 IP 登录 | 被 `LOGIN_IP_ALLOWLIST` 拒绝 | | |

---

## 2. 用户管理（Admin）

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 2.1 | admin 创建律师用户 | 用户列表可见；初始密码生效 | | |
| 2.2 | admin 重置律师密码 | 律师被 global signOut；`requires_password_change=true` | | |
| 2.3 | 律师首登强制改密 | 改密后进入 `/drive` 或默认业务页 | | |
| 2.4 | admin 禁用用户 | 用户无法登录 | | |
| 2.5 | 尝试禁用内置 admin | 操作被拒绝 | | |

---

## 3. 转写上传与流水线

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 3.1 | 律师上传小体积 MP3（< 10MB） | init → TUS → complete；列表见 `queued` | | |
| 3.2 | 等待 Worker 处理 | 状态经 `preprocessing` → `asr_running` → `llm_running` → `completed` | | |
| 3.3 | 上传 MP4 文件 | 经 `extracting` 阶段；完成后仅有音频流水线 | | |
| 3.4 | 上传进行中关闭浏览器 tab | `beforeunload` 警告出现 | | |
| 3.5 | 上传进行中 SPA 路由离开 | `AlertDialog` 拦截 | | |
| 3.6 | 单文件 > 1GB 尝试上传 | 被 init 或 TUS 拒绝 | | |

---

## 4. 转写工作台

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 4.1 | 打开已完成任务工作台 | 校对模式可见 `asr_raw_json` 段落 | | |
| 4.2 | 点击段落 seek | 音频 `currentTime` 跳转至对应时间 | | |
| 4.3 | 切换编辑模式，修改 `polished_text` 并保存 | `If-Match` 乐观锁成功；版本 +1 | | |
| 4.4 | 导出 Word (.docx) | 浏览器下载有效 docx 文件 | | |
| 4.5 | 导出 PDF / TXT | 下载成功且内容正确 | | |

---

## 5. 云盘

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 5.1 | 任务完成后检查云盘 | 自动创建 `YYYY-MM-DD/任务名/` 目录 | | |
| 5.2 | 全文检索转写正文关键词 | 命中对应任务/文件 | | |
| 5.3 | 下载云盘文件 | BFF 签名 URL + 审计 `file.download` | | |
| 5.4 | 创建子文件夹、上传限制验证 | 根目录不可直接堆文件 | | |

---

## 6. 审计与 Admin

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 6.1 | admin 审计列表筛选 `auth.login_success` | 可见最近登录记录 | | |
| 6.2 | 筛选 `file.download` | 可见下载审计；含 `client_timestamp` | | |
| 6.3 | 筛选 `auth.login_failure` | 可见 `attempted_username`（掩码规则待 PRD 闭合） | | |
| 6.4 | 律师访问 `/admin/audit` | 403 或重定向拒绝 | | |

---

## 7. AI 配置（Admin）

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 7.1 | 添加 ASR/LLM 模型凭证 | 加密存储；列表不展示明文 Key | | |
| 7.2 | 连通性测试 | 返回成功/失败状态 | | |
| 7.3 | 功能映射 + Prompt 模板 CRUD | 保存成功；Worker 可使用新配置 | | |

---

## 8. 私有化就绪

| # | 步骤 | 预期 | 结果 | 备注 |
|---|------|------|:----:|------|
| 8.1 | `npm run test:compliance` | 全绿 | | |
| 8.2 | `npm run check:privatization` | 全绿 | | |
| 8.3 | `GET /health` | postgres + storage 均 ok | | |
| 8.4 | Worker 启动日志 | FFmpeg 版本行可见 | | |
| 8.5 | 核对 `DEPLOYMENT.md` §7 矩阵 | 全部勾选 | | |

---

## 9. 验收汇总

| 字段 | 值 |
|------|-----|
| 验收日期 | |
| 环境 | development / staging / production |
| 验收人 | |
| 通过项 / 总项 | / |
| 阻塞问题 | |
| 关联 `OPEN_ISSUES.md` 签收 | |
