{
  "name": "lexos",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "workers/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:supabase": "vitest run packages/shared/src/config/supabase-connectivity.test.ts",
    "verify:supabase-cli": "vitest run packages/shared/src/config/supabase-cli.test.ts",
    "seed:admin": "npx tsx packages/shared/src/seed/run-builtin-admin-cli.ts",
    "seed:sql": "supabase db query --linked -f supabase/seed.sql"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "dotenv": "^16.5.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
